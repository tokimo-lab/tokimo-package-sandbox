/target
/target-staging
Cargo.lock
*.swp
.DS_Store
.cargo/
# Local test rootfs (downloaded by CI / dev via TOKIMO_TEST_ROOTFS env)
/.test-rootfs/
# Windows VM artifacts (downloaded by scripts/fetch-vm.ps1)
/vm/
# Compiled guest binaries (built by packaging/vm-local/build-init-bin.sh)
/packaging/vm-local/tokimo-sandbox-init
/packaging/vm-local/tokimo-tun-pump
/packaging/vm-local/tokimo-sandbox-fuse
# IDE / agent config (per-developer)
/.claude/
/.ida-mcp/
/packaging/vm-base/tokimo-os-arm64/
.github/copilot-instructions.md

# Per-user IDE config
/.vscode/
.cargo-docker/
.rustup-docker/

# Ephemeral rootfs VHDX clones left by crashed/killed service
.tokimo-rootfs-*.vhdx
